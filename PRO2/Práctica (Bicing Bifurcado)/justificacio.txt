Justificació

Iterativa (Subir bicis):
    ·Invariant del bucle
    ·Justificació:
        -Inicialització
        -Condició de sortida
        -Cos del bucle
        -Acabament

Recursiva (Asignar estación (const BinTree<string>& a, const string& ide_mejor, int& coef_max, int& pl, int& nest)):
    ·Hipótesis de inducción
        ae_rec(a.left(), id_mejor, coef_max, pl izq, nest izq);
        ae_rec(a.right(), id_mejor, coef_max, pl der, nest der);
    ·Justificació:
        -Caso base: si el árbol está vacío (no tiene estaciones ni plazas libres)
        -Caso recursivo
        -Acabament